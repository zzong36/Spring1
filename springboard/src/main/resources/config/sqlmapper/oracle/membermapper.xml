<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member.dao.memberDAO">

	<select id="selectUser" parameterType="memberVO"
		resultType="memberVO">
		select id, password, name from t_member where id=#{id} and
		password=#{password}
	</select>

	<insert id="insertUser" parameterType="memberVO">
		insert into t_member(id, password, name) values(#{id}, #{password}, #{name})
	</insert>
</mapper>